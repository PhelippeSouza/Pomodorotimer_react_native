import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  pickerContainer: {
    marginTop: 10,
  },

  pickerStyle: {
    marginTop: "1%",
    marginBottom: 4,
    fontSize: 10,
  },
});

export default styles;
